
    create table User4Java (
        id bigint generated by default as identity,
        created timestamp,
        name varchar(255),
        updated timestamp,
        version timestamp,
        primary key (id)
    );
