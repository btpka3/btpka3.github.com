[source,bash]
----
docker create                                       \
    --name my-mysql                                 \
    --restart unless-stopped                        \
    -it                                             \
    -e MYSQL_ROOT_PASSWORD=123456                   \
    -v /Users/zll/data0/store/soft/mysql:/var/lib/mysql                  \
    -p 3306:3306                                    \
    mysql:5.7.21

# 通过docker 本地启动 mysql 数据库，如何创建，咱时省略。
docker start my-mysql


brew install mysql
# https://github.com/sakiladb/mysql
mysql --host=127.0.0.1 --port=3306 --user=root --password=123456 < 1-sakila-schema.sql
mysql --host=127.0.0.1 --port=3306 --user=root --password=123456 < 2-sakila-data.sql

----

* https://www.jooq.org/notes[The jOOQ Release Note History]
** 3.17.0 : OSS 版本需要 JDK 17
** 3.15.0 : OSS 版本需要 JDK 11
* https://www.jooq.org/download/support-matrix[Version Support Matrix]
* 3.16 https://www.jooq.org/doc/3.16/manual-single-page/[文档]：
** 自定义数据类型的绑定:
https://www.jooq.org/doc/3.16/manual-single-page/#custom-bindings[4.22.5.
Custom data type bindings]
** https://www.jooq.org/doc/3.16/manual-single-page/#codegen-database-forced-types-converter[6.2.5.15.3.
Qualified converters]
* gradle plugin :
https://github.com/etiennestuder/gradle-jooq-plugin[nu.studer.jooq]
** org.jooq:jooq-codegen
*** org.jooq.meta.jaxb.Configuration
* 示例数据库： https://github.com/sakiladb/mysql[sakiladb/mysql]
* docker 镜像: https://hub.docker.com/r/sakiladb/mysql[sakiladb/mysql]
* org.jooq.meta.jaxb.Configuration
* {blank}
+
== 启动数据库

[source,bash]
----

cd src/test/resoures
docker-compose up
----

== 生成代码

[source,bash]
----
./gradlew clean build
----
