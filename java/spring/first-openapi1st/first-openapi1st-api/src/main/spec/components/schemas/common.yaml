ResourceId:
  type: integer
  format: int64
  description: Unique resource identifier

Timestamp:
  type: string
  format: date-time
  description: ISO 8601 timestamp

SortOrder:
  type: string
  enum:
    - ASC
    - DESC
  default: ASC
