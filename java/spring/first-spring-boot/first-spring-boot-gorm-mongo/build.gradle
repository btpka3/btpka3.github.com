

dependencies {
    //compile('org.grails:grails-datastore-gorm-rx-mongodb')
    //compile "org.grails.plugins:rx-mongodb"
    compile("org.grails:gorm-mongodb-spring-boot")
    compile("org.grails:grails-validation")
}

test {
    include 'me/test/**'
    testLogging.showStandardStreams = false
}

